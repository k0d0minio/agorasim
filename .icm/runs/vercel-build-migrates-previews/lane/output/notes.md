# Chore: vercel-build-migrates-previews

- invariant: production deploys behave exactly as before — `next build`, no migration step, `db-migrate.yml` the one production migrator; only preview deployments (every PR's, and the `uat` branch's) differ: they apply the Drizzle journal to their own Neon branch and verify it before `next build`
- change: web/scripts/vercel-build.sh (new) + web/package.json `vercel-build` script — Vercel runs it in place of `build`; `VERCEL_ENV=preview` → `pnpm db:migrate && pnpm db:verify` over `DATABASE_URL_UNPOOLED`, then `pnpm build`; `production`, or an unexposed `VERCEL_ENV`, → build only, said aloud. `.icm/_shared/project-rules.md` → The environments' databases records the choice and the acts now done (toggle on, key in the Actions secrets)
- rollback: revert the two `web/` files — the script is additive, touches no schema and no data, so a revert only stops previews migrating themselves. Not a session's own decision: a revert is the hotfix lane's, prepared by `rollback.sh`, merged by the operator. Forward-only repo: nothing for reverted code to tolerate — the migrations this step applies are each branch's own
- learned: none
- parked: .icm/intake/triage/dependency-advisories-2026-09-23.md — pnpm audit's 17 pre-existing high/critical advisories (next <16.3.3 is the critical one), not this chore's diff; the gate ran with --no-audit after parking it
