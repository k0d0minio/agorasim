# Build notes: admin-quote-builder

- commits: e39475d message_log per quote (+ migration 0027) · 8b8e3c7 send/re-send/new version/discard + token + TERMS_VERSION + quote-builder · 496d65a quote-sent email · efbbf56 the Orçamento card, actions, form schema
- ci: cheap tier GREEN on 1f01108 (after one RED: a test-helper type error, error.log); full gate on the post-flip head

## What changed

- `web/src/db/schema.ts`, `web/drizzle/0027_quote_sent_per_quote.sql`: `message_log.quote_id` (FK quotes, cascade) and `quote_sent_at`; unique index `message_log_quote_kind_key` on (kind, recipient, quote_id, quote_sent_at); `message_log_enquiry_kind_key` narrowed to `quote_id is null`; index on `quote_id`. Generated by drizzle-kit, applied and verified (28/28) on the run's Neon branch `run/admin-quote-builder`.
- `web/src/lib/message-log.ts`: a quote-shaped `MessageSubject` (`QUOTE_SEND_KINDS = ["quote-sent"]`), requiring `quoteId` + `quoteSentAt`.
- `web/src/lib/quote-math.ts` (new, client-safe): `splitTotal`, `lineItemsTotal`, the two constants moved out of `quotes.ts` (re-exported there, no caller changed) + `balanceDueKey` for the live preview.
- `web/src/lib/quotes.ts`: `markQuoteSent` gains `from` / `ifSentAt` guards in its `WHERE`; `supersedeSentQuotes`, `discardDraft`, `copyQuoteAsDraft`; pure `canStartQuote`, `canCopyAsNewVersion`, `wasSuperseded`.
- `web/src/lib/quote-token.ts` (new): 32-byte token, HMAC under `BOOKING_TOKEN_SECRET` with a `quote:` domain prefix — no new env var; `quotePath(locale, token)` → `/<locale>/orcamento/<token>`.
- `web/src/content/terms.ts`: `TERMS_VERSION = "2026-09-10"`, pinned to the printed "last updated" by `terms.test.ts`.
- `web/src/lib/quote-builder.ts` (new): create / save / discard / new version / send / re-send, each returning an outcome; audit rows on the lead (`quote.created|updated|discarded|sent|resent|superseded`, added to `AUDIT_ACTIONS` with PT labels); `quoteEmailStates` for the card.
- `web/src/content/emails.ts`, `web/src/lib/booking-emails.ts`: `guestQuoteSentEmail`, PT/EN, HTML + text.
- `web/src/lib/form-schemas.ts`: `quoteDraftSchema` (lines from three repeated fields, blank row ignored, the incomplete row named), `quoteIdSchema`, `resendQuoteSchema`.
- `web/src/app/admin/sales/actions.ts`: `saveQuoteDraft`, `discardQuote`, `newQuoteVersion`, `sendLeadQuote`, `resendLeadQuote` — `requireAdmin()` first.
- `web/src/components/admin/lead-quote-card.tsx` (new) mounted in `web/src/app/admin/sales/[id]/page.tsx` for wedding/event leads that are not anonymised; `web/src/lib/admin-format.ts` quote/instalment labels.

## Acceptance criteria status

- [x] Draft prefilled from the enquiry (venue, language, date when a calendar day, deposit 30%); no card on a tour lead — `page.tsx` mount + `createDraftForLead`.
- [x] Lines: label, quantity, unit price in €; live total/deposit/balance/due date (`quote-math`); stored in cents; no line / zero total / no date refused with the reason (`quoteDraftSchema`, `builderProblems`).
- [x] Draft editable and discardable; sent/paid/cancelled show no edit control.
- [x] Send stamps `TERMS_VERSION`, stores only the digest, moves the lead to Orçamentado with a Histórico row (existing `moveLeadStage`), emails once in the quote's language with the amounts and the link.
- [x] Failed email leaves the quote sent and the card says so (`quoteEmailStates`); "Reenviar" rotates the digest and emails once more.
- [x] "Nova versão" → copied draft; its send cancels the old sent quote (Substituído); one draft / one live per lead; none on deposit-paid or paid.
- [x] Card shows ref, status, date, venue, lines, total, sent date, terms version, Sinal and Saldo rows with amounts, due date, state.
- [x] Second version's email not refused (quote-shaped index), double submit sends once (`from`/`ifSentAt` guards + log key), failed attempt retryable; lead deletion cascades to the log rows.
- [x] Unit tests: `quote-builder.test.ts`, `quote-writes.test.ts` (SQL guards), `quotes.test.ts` (rules), `message-log.test.ts`, `booking-emails.test.ts`, `form-schemas.test.ts`, `quote-token.test.ts`, `quote-math.test.ts`, `terms.test.ts`.
- [ ] Glossary strings, ≥44px targets, ≥12px text — met (default `h-11`/`h-12` controls, nothing below `text-xs`); **the phone guide half cannot be met**: `web/docs/guia-telemovel.md` was deleted in `6cbf0d4` (2026-09-18). Not recreated; parked as `intake/triage/knowledge-map-web-docs-deleted.md`. CI: see status.md.

## Notes for Release

- **Spec gap (D-6):** the phone guide and the admin design spec no longer exist; the knowledge map still routes to them. Release has no page to update for the admin screen — the triage stub carries the decision.
- **Migration 0027 touches the enquiry index the enquiry ack relies on.** Drop-and-recreate with a narrower predicate, inside drizzle's per-file transaction; the new predicate is a subset of the old, so existing rows cannot violate it. Forward-only (`migrations.reversible: false`); the code tolerates the newer schema.
- **The emailed link 404s until `quote-page-and-deposit-link` ships** (D-4, accepted).
- A preview built after the ready flip applies 0027 to its own `preview/<branch>` database (`vercel-build.sh`); production takes it from `db-migrate.yml` after the promotion merges into `main`.
- Sending needs `BOOKING_TOKEN_SECRET` (already required for cancel links) and Resend; without the secret the action says so and sends nothing.
- `quote-math.ts` duplicates the T−14 date shift for the browser; `quote-math.test.ts` pins it to the server's `balanceDueDate`.
