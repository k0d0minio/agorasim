# Handoff: fix-placeholder-contrast

For the next session — human or agent — what to do first and what stands in the way.
Rewritten, not appended, at every stage stop; a stage that STOPs mid-way writes it before it
stops, so nothing is carried in anyone's head.

## Next steps

1. PR open — smoke, then squash-merge from GitHub.

## Blockers

- blocked on operator: smoke-test the preview, then squash-merge PR #143 from GitHub.

A blocking operator act is written here **and** in the stop report's `Operator:` list; a
non-blocking one lives only in that list, never here (`_shared/output.md` → Split by actor).

## Do not

- Do not re-invoke the `bug` lane for this slug — one invocation, one PR; the merge is the gate.
