# Handoff: day-before-reminder

For the next session — human or agent — what to do first and what stands in the way.
Rewritten, not appended, at every stage stop; a stage that STOPs mid-way writes it before it
stops, so nothing is carried in anyone's head.

## Next steps

1. Merged into `uat` and archived; nothing to pick up. It reaches production with the UAT
   batch's promotion (`uat status`).

## Blockers

- none.

## Do not

- Re-open this run. Follow-ups are `triage/booking-logistics-facts-shared` and the next
  lifecycle stub, `thankyou-review-email`.
